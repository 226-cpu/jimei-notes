# 使用说明

## 启动

在 **`笔记` 文件夹**里打开终端（资源管理器地址栏输入 `cmd` 或 PowerShell），依次执行：

```bash
npm install
npm run dev
```

浏览器打开终端里显示的地址（一般是 `http://localhost:5173`）。

### 提示「vitepress 不是内部或外部命令」

常见原因：

1. **还没安装依赖** — 必须先在本文件夹执行 `npm install`，等其完成后再 `npm run dev`。
2. **直接输入了 `vitepress dev`** — 不要单独敲 `vitepress`，请用 **`npm run dev`**（会通过项目里的依赖调用）。
3. **终端不在项目目录** — 先 `cd` 到含有 `package.json` 的 `笔记` 文件夹再执行上述命令。

若 `npm install` 报错，请先安装 [Node.js](https://nodejs.org/)（选 LTS），安装后**重新打开终端**再试。

仍不行时可试：

```bash
npx vitepress dev
```

（同样需要在已执行过 `npm install` 的 `笔记` 目录下。）

---

## 新增一门课程（如线性代数、四级词汇）

1. **建文件夹**（与课程名一致），例如：

   ```
   笔记/线性代数/
   ```

2. **课程首页**（可选但推荐）`线性代数/index.md`：

   ```md
   # 线性代数

   本课程笔记目录。
   ```

3. **在 `.vitepress/courses.ts` 里注册**，在 `courses` 数组末尾增加：

   ```ts
   {
     folder: '线性代数',
     name: '线性代数',
     category: '数学',  // 思政 | 数学 | 英语 | 专业课 | 其他
     chapters: [],
   },
   ```

4. **更新本站首页** `index.md` 的「课程目录」里加一行链接（方便自己浏览）。

保存后刷新浏览器，顶栏会出现新课程。

---

## 新增一章 / 一篇笔记

1. 在课程文件夹下新建 `.md`，例如：

   `高等数学/第一章-极限与连续.md`

2. 在 `courses.ts` 里，找到该课程的 `chapters`，增加：

   ```ts
   { text: '第一章 极限与连续', link: '/高等数学/第一章-极限与连续' },
   ```

3. 若该课程已有章节，侧栏会自动列出；顶栏会指向该课程**第一篇**笔记。

---

## 不同学科的笔记结构（可参考）

| 类型 | 建议区块 |
|------|----------|
| 思政 | 本节主题、必背表述、理解要点、易混辨析、复习自测 |
| 数学 | 定义与定理、例题、易错点、公式卡片 |
| 英语 | 词汇、短语、例句、听力/阅读要点 |

模板文件在 `templates/` 目录，复制后改名使用即可。

---

## 文字颜色（样式类）

在任意 `.md` 里用 HTML 包裹文字即可（保存后刷新预览）：

| 样式类 | 用途 | 写法 |
|--------|------|------|
| `必背` | 必须按原文记的内容 | `<span class="必背">两个结合</span>` |
| `高亮` | 黄底强调、复习重点 | `<span class="高亮">考试重点</span>` |
| `术语` | 专有名词、核心概念 | `<span class="术语">马克思主义中国化</span>` |
| `易混` | 容易记混的概念 | `<span class="易混">新时代</span>` |

示例：

```markdown
<span class="必背">两个结合</span>：把马克思主义基本原理同
<span class="术语">中国具体实际</span>相结合、同
<span class="术语">中华优秀传统文化</span>相结合。

<span class="易混">新时代</span> 与 <span class="易混">新时期</span> 的起止时间不同。
```

大块内容仍建议用提示框：`::: tip` / `::: warning` / `::: danger` / `::: info`（见第一讲文末示例）。

样式定义在 `.vitepress/theme/custom.css`，可按喜好自行改颜色。

---

## 快速写表格（不必手敲每一根 `|`）

> 代码片段**不能**做到 `note-table5` 自动识别数字（VS Code 限制）。**列数任意**请用下面的「方法一」。

### 方法一：按你输入的数字生成列数（推荐）

**A. 命令面板（会问你填几列）**

1. 用 Cursor **打开文件夹** `笔记`
2. `Ctrl + Shift + P` → 输入 **Tasks: Run Task**（运行任务）
3. 选 **生成 Markdown 表格（剪贴板）**
4. 输入列数（如 `5`）、数据行数（如 `3`）
5. 回到 `.md`，**Ctrl + V** 粘贴

**B. 终端一行命令**

在 `笔记` 目录：

```bash
npm run table -- 5
npm run table -- 5 3
```

第一个数字是**列数**，第二个可选是**数据行数**（默认 2）。生成后会尽量自动复制到剪贴板。

### 方法二：固定列数的代码片段（2 列 / 3 列）

适合常做的易混两列表、三列表，输入 `table2` / `note-table3` 等，用 **Ctrl+Space** 选 **【集美笔记】** 那条，**Enter** 插入（Tab 在 Cursor 里可能被 AI 占用）。

| 输入 | 生成 |
|------|------|
| `table2` / `note-table2` | 固定 2 列 |
| `table3` / `note-table3` | 固定 3 列 |

片段不生效时：确认打开的是「笔记」文件夹 → **Reload Window**。

### 方法三：Cursor / VS Code 插件

安装 **Markdown All in One** 后：

- `Ctrl+Shift+P` → **Create Table** 可指定行、列数自动生成
- 选中已有表格 → **Format Table** 自动对齐 `|`

### 方法四：在线生成后粘贴

打开 [Tables Generator · Markdown](https://www.tablesgenerator.com/markdown_tables)，像 Excel 一样填格子，复制生成的 Markdown 贴进笔记。

### 不必强行用表格时

只有两三点对比，用列表往往更快：

```markdown
- **新时代**：……
- **新时期**：……
```

---

## 在手机上查看

### 方式一：同一 WiFi 局域网（不用部署，适合宿舍/家里）

电脑和手机连**同一个 WiFi**，在 `笔记` 文件夹执行：

```bash
npm run dev:lan
```

终端里会出现两行地址，例如：

- `http://localhost:5173`（仅电脑）
- `http://192.168.1.100:5173`（局域网，**手机用这个**）

在手机浏览器输入带 **192.168…** 的那一行（你机器上的 IP 可能不同，以终端为准）。

注意：

- 执行期间**不要关**电脑终端；电脑休眠或断网，手机会打不开。
- 第一次可能被 **Windows 防火墙** 拦截，弹出提示时选「允许访问」。
- 仍打不开：设置 → 网络和 Internet → WLAN → 当前 WiFi → 设为「专用网络」，再试。

电脑改完笔记并保存后，手机**刷新页面**即可看到更新。

---

### 方式二：先打包再局域网预览（稍稳、稍快）

```bash
npm run build
npm run preview:lan
```

手机同样访问终端里的 `http://192.168.x.x:4173`（端口可能是 4173，以终端为准）。适合复习时长时间看手机，不依赖 dev 热更新。

---

### 方式三：部署到公网（出门也能看）

不在同一 WiFi 时，把站点发布到 GitHub Pages 或 Cloudflare Pages。

**完整步骤（建仓库、Git、自动部署、手机访问）见 → [部署教程](/deploy)**

---

### 小结

| 场景 | 做法 |
|------|------|
| 宿舍/家，手机和电脑同 WiFi | `npm run dev:lan`，手机浏览器打开局域网地址 |
| 复习时长时间刷手机 | `npm run build` + `npm run preview:lan` |
| 随时随地、不在同一网络 | [部署教程](/deploy) → GitHub Pages |

---

## 公网部署

详见 **[部署教程](/deploy)**（GitHub Pages 逐步说明 + Cloudflare Pages）。

### `npm run build` 报错时

先运行自检（不通过就不要 build）：

```bash
npm run check
```

1. **看终端最后几行**，把完整报错复制下来排查。
2. **`:::` 必须从行首开始**，前面不能有空格（缩进会导致解析失败）。
3. **提示框未闭合**（最常见）：每个 `::: info` / `::: warning` 等块末尾必须有单独一行的 `:::` 再开下一块。例如：

```markdown
::: info 标题
正文……
:::

::: warning 下一块
……
:::
```

4. **`cursor_.md`** 已排除在构建之外，勿放进课程目录当笔记。
5. **`dead link(s) found`**：多为链到 `templates/` 或 `.ts` 文件；模板只在本地用，不要写成 `[文字](../templates/xxx.md)`。
6. 修好后重新执行 `npm run build`。


::: tip 理解要点
用自己的话概括即可。
:::

::: warning 易混
「新时代」和「新时期」不要记混。
:::

::: danger 必背
必须按教材表述记忆的内容。
:::

::: info 补充
老师拓展、时政材料等。
:::

| 列1 | 列2 | 列3 |
| --- | --- | --- |
|  |  |  |


<!-- <span style="background: #c0392b; color: #fff;"></span> -->

<!-- <span style="background: #fef08a"></span> -->